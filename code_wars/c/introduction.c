#include <stdlib.h>
#include <stdio.h>
#include <string.h>

struct Node
{
    int value;
    struct Node *next;
};

struct Node *counter_effect(const char *hit_count, size_t *p_result_count)
{
    int toMalloc = 0
    for
    return NULL;
}

int main(void)
{
    counter_effect("1345", 4);
}